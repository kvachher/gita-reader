services:
  - type: web
    name: gita-reader
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python scripts/regenerate.py "MM19 Gita.xlsx" --out output
    startCommand: gunicorn gita_reader.web:app
    autoDeploy: true
